# Agent Bar 10.6.2

Automated release cut from main. Changes since 10.6.1:

- fix(reset): report an unconfirmed claim as typed data
- docs(spec): add the unconfirmed reset caption
- fix(resets): close a stale confirm, drop nowMs
- test(service): cover the reset claim flow
- fix(service): read an ambiguous claim as unconfirmed
- fix(service): keep reset caption past its refresh
- fix(service): keep a reset claim across close
- docs: renumber reset rules to CLI-032..037
- fix(reset): post the raw cedar_ember grant id
- fix(reset): use the catalog Claude timeout
- fix(reset): reject unclaimable ids before any I/O
- fix(reset): validate the organization uuid
- fix(reset): parse claim responses leniently
- fix(reset): retry the usage GET like collection
- refactor(claude): share request headers and creds
- fix(claude): tolerate malformed reset blocks
- test: keep credits banned in assert_no_money
- fix(reset): make request_id a valid UUID v4
- fix(popup): use a text button for the reset claim
- fix(core): use time format for reset cooldown text
- test: cover usage resets end to end
- feat(service): wire the reset command and confirm dialog
- feat(popup): render the Resets section
- feat(core): model usage resets in status and helper argv
- docs: document banked usage resets and reset command
- feat(cli): add reset claude command
- feat(http): add HttpClient::post seam
- feat(claude): identify as claude-cli and map banked resets
- feat(status): add UsageReset and Codex reset mapping
- docs(spec): amend banked usage resets design
